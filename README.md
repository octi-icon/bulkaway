# Bulk Away website

The website is in `website/`. See [website/README.md](website/README.md) for local preview, checks, Google email configuration, and Render deployment.

For deployment, follow [RELEASE.md](RELEASE.md): exact Render settings, environment variables, launch checks, and rollback steps.

`render.yaml` configures the requested Render Node service. Brand artwork and fonts are supplied by the owner. The reference PDF remains untouched.
